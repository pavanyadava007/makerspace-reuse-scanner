model: /models/yolo11n_makerspace.onnx
classes: /models/classes.yaml
camera: 0            # int index, /dev/video path, or RTSP URL
imgsz: 640
min_conf: 0.35
iou: 0.5
send_every_n: 3      # push every 3rd frame's detections over WS
crop_upload: true    # upload crops for VLM material stage
